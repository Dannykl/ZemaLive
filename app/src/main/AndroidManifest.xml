<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="live.zema.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".ui.features.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.features.login.LoginActivity" />
        <activity android:name=".ui.features.Reset.ResetPassword" />
        <activity android:name=".ui.features.signup.SignupActivity" />
        <activity android:name=".ui.features.home.HomeActivity" />
        <activity android:name=".ui.features.album.AlbumDetails" />
        <activity android:name=".ui.features.album.AlbumInfo" />
        <activity android:name=".ui.features.more.more" />
        <activity android:name=".ui.features.search.AlbumSearch" />
    </application>

</manifest>